/* 全局样式 */
* {
    font-family: "Segoe UI", "Microsoft YaHei UI", sans-serif;
    font-size: 14px;
    color: #202020;
}

/* 窗口和容器样式 */
QMainWindow, #mainContainer {
    background-color: #fafafa;
    border: 1px solid #e0e0e0;
    border-radius: 8px;
}

QMainWindow::title {
    background-color: #fafafa;
    color: #202020;
}

#centralWidget {
    background-color: #fafafa;
    border: none;
}

/* 标题区域样式 */
#titleBar {
    background-color: #f5f5f5;
    border-top-left-radius: 10px;
    border-top-right-radius: 10px;
    border-bottom: 1px solid #e0e0e0;
}

#titleFrame {
    border-bottom: 1px solid #e0e0e0;
    background-color: transparent;
}

#windowTitle {
    font-size: 14px;
    font-weight: 500;
    color: #404040;
}

#titleLabel {
    color: #202020;
    font-size: 24px;
    font-weight: 600;
    margin-bottom: 4px;
}

#sectionLabel {
    color: #404040;
    font-size: 16px;
    font-weight: 500;
    margin-top: 5px;
    margin-bottom: 8px;
}

/* 新提醒框 */
#newReminderFrame {
    background-color: white;
    border-radius: 8px;
    border: 1px solid #e0e0e0;
}

/* 按钮通用样式 */
QPushButton {
    border-radius: 5px;
    padding: 8px 16px;
    min-width: 80px;
    font-size: 14px;
    font-weight: 500;
}

/* 主按钮样式 */
#primaryButton {
    background-color: #0078d4;
    color: white;
    border: none;
}
#primaryButton:hover { background-color: #106ebe; }
#primaryButton:pressed { background-color: #005a9e; }

/* 次要按钮样式 */
#secondaryButton {
    background-color: #e1e1e1;
    color: #202020;
    border: none;
}
#secondaryButton:hover { background-color: #d1d1d1; }
#secondaryButton:pressed { background-color: #c1c1c1; }

/* 操作按钮样式 */
#actionButton {
    background-color: white;
    color: #0078d4;
    border: 1px solid #0078d4;
}
#actionButton:hover { background-color: #f0f7fd; }
#actionButton:pressed { background-color: #e5f1fb; }

/* 输入控件样式 */
QLineEdit, QTimeEdit, QSpinBox {
    border: 1px solid #d1d1d1;
    border-radius: 4px;
    padding: 8px;
    background-color: white;
    color: #333333;
    font-size: 14px;
    min-height: 20px;
    padding-right: 15px; /* 留出空间给上下箭头 */
}

QLineEdit:focus, QTimeEdit:focus, QSpinBox:focus {
    border-color: #0078d4;
}

/* 隐藏输入框上下按钮 */
QSpinBox::up-button, QSpinBox::down-button,
QTimeEdit::up-button, QTimeEdit::down-button,
QDoubleSpinBox::up-button, QDoubleSpinBox::down-button,
QDateEdit::up-button, QDateEdit::down-button,
QDateTimeEdit::up-button, QDateTimeEdit::down-button {
    width: 0px;
    height: 0px;
    border: none;
    background: transparent;
}

/* 列表样式 */
QListWidget {
    background-color: white;
    border: 1px solid #e0e0e0;
    border-radius: 6px;
    padding: 5px;
    outline: none;
    font-size: 14px;
    color: #333333;
}

QListWidget::item {
    padding: 10px;
    border-radius: 3px;
    margin: 2px 0px;
}

QListWidget::item:selected {
    background-color: #e5f1fb;
    color: #0078d4;
    border: none;
}

QListWidget::item:hover:!selected {
    background-color: #f5f5f5;
}

/* 菜单栏样式 */
QMenuBar {
    background-color: #f5f5f5;
    border-bottom: 1px solid #e0e0e0;
    padding: 2px 0px;
    min-height: 28px;
    font-size: 13px;
}

QMenuBar::item {
    background-color: transparent;
    padding: 4px 10px;
    margin: 1px 4px;
    border-radius: 4px;
    color: #505050;
}

QMenuBar::item:selected {
    background-color: rgba(0, 120, 212, 0.1);
    color: #0078d4;
}

QMenuBar::item:pressed {
    background-color: rgba(0, 120, 212, 0.15);
    color: #0078d4;
}

/* 菜单样式 */
QMenu {
    background-color: #ffffff;
    border: 1px solid #e0e0e0;
    border-radius: 8px;
    padding: 4px;
    color: #333333;
    font-size: 14px;
    margin: 2px 0px 0px 0px;
}

QMenu::item {
    padding: 6px 32px 6px 16px;
    border-radius: 4px;
    margin: 2px 4px;
    min-width: 120px;
}

QMenu::item:selected {
    background-color: rgba(0, 120, 212, 0.1);
    color: #0078d4;
}

QMenu::separator {
    height: 1px;
    background-color: #e0e0e0;
    margin: 4px 8px;
}

QMenu::icon {
    margin: 0px 8px 0px 4px;
}

QMenu::indicator {
    width: 16px;
    height: 16px;
    margin-left: 8px;
}

QMenu::indicator:non-exclusive:checked {
    image: url(:/images/checked.png);
    padding-left: 2px;
}

/* 子菜单样式 */
QMenu QMenu {
    border-top-right-radius: 8px;
    border-bottom-right-radius: 8px;
    margin: -2px 0px 0px 0px;
}

QMenu::right-arrow {
    image: url(:/images/arrow-right.png);
    margin-right: 4px;
}

/* 标题栏按钮样式 */
QAbstractButton#qt_titlebar_minimizebutton, 
QAbstractButton#qt_titlebar_maximizebutton, 
QAbstractButton#qt_titlebar_closebutton {
    background-color: transparent;
    border: none;
    border-radius: 0px;
    margin: 0px;
    padding: 0px;
}

QAbstractButton#qt_titlebar_minimizebutton:hover, 
QAbstractButton#qt_titlebar_maximizebutton:hover {
    background-color: rgba(0, 0, 0, 0.05);
}

QAbstractButton#qt_titlebar_closebutton:hover {
    background-color: #e81123;
}

/* 标题栏图标和文本 */
QWidget#qt_titlebar_label {
    font-size: 13px;
    font-weight: normal;
    color: #505050;
    padding-left: 8px;
}

/* 标签样式 */
QLabel {
    color: #202020;
    font-size: 14px;
}

/* 设置表单标签对齐 */
QFormLayout QLabel {
    padding-right: 10px;
    font-weight: 500;
}

/* 工具提示样式 */
QToolTip {
    background-color: #ffffff;
    color: #333333;
    border: 1px solid #d1d1d1;
    border-radius: 4px;
    padding: 6px 8px;
    font-size: 13px;
}

/* 滚动条样式 */
QScrollBar:vertical {
    border: none;
    background: #f0f0f0;
    width: 8px;
    border-radius: 4px;
    margin: 0px;
}

QScrollBar::handle:vertical {
    background: #c0c0c0;
    border-radius: 4px;
    min-height: 20px;
}

QScrollBar::handle:vertical:hover {
    background: #a0a0a0;
}

QScrollBar::add-line:vertical, QScrollBar::sub-line:vertical {
    background: none;
    height: 0px;
    subcontrol-position: top;
    subcontrol-origin: margin;
}

/* 对话框样式 */
QDialog {
    background-color: #fafafa;
    border: 1px solid #e0e0e0;
    border-radius: 8px;
}

QDialog QLabel {
    color: #333333;
}

QDialog QPushButton {
    min-width: 88px;
}
